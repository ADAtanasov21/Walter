#pragma once

#include "raylib.h"

void playMenu();

typedef enum {
    BACKPLAY
}backPlayButton;

static const char* backPlayText = "Back";

const int screenWidth = 1250;
const int screenHeight = 800;